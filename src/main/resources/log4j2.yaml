Configuration:
  status: warn
  name: ErisLogger
  packages: "org.javalite.logging"
  properties:
    property:
      name: log-path
      value: "logs"
  appenders:
    Console:
      name: Console
      target: SYSTEM_OUT
      PatternLayout:
        Pattern: "ERIS: [%level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %C %M - %msg%n"
    File:
      name: File
      fileName: ${log-path}/logfile.log
      PatternLayout:
        pattern: "ERIS: [%level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] - %msg%n"
    RollingFile:
      - name: RollingFile
        fileName: ${log-path}/rollingfile.log
        filePattern: "logs/archive/rollingfile.log.%d{yyyy-MM-dd-hh-mm}.gz"
        PatternLayout:
          pattern: "ERIS: [%level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] - %msg%n"
        Policies:
          SizeBasedTriggeringPolicy:
            size: 1 MB
        DefaultRollOverStrategy:
          max: 2000

  Loggers:
    Root:
      level: info
      AppenderRef:
        ref: Console
    Logger:
      - name: dev.laarryy.eris
        level: info
        AppenderRef:
          - ref: File
          - ref: RollingFile
            level: info
      - name: org.javalite
        level: warn
        AppenderRef:
          - ref: Console
            level: warn
      - name: discord4j.rest
        level: debug
        AppenderRef:
          - ref: Console
            level: debug