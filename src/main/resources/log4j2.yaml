Configuration:
  status: warn
  name: Iciclogger
  properties:
    property:
      name: log-path
      value: "logs"
  appenders:
    Console:
      name: Console
      target: SYSTEM_OUT
      PatternLayout:
        Pattern: "ERIS: [%level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] - %msg%n"
    File:
      name: File
      fileName: ${log-path}/logfile.log
      PatternLayout:
        pattern: "ERIS: [%level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] - %msg%n"
    RollingFile:
      - name: RollingFile
        fileName: ${log-path}/rollingfile.log
        filePattern: "logs/archive/rollingfile.log.%d{yyyy-MM-dd-hh-mm}.gz"
        PatternLayout:
          pattern: "ERIS: [%level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] - %msg%n"
        Policies:
          SizeBasedTriggeringPolicy:
            size: 1 MB
        DefaultRollOverStrategy:
          max: 2000

  Loggers:
    Root:
      level: info
      AppenderRef:
        ref: Console
    Logger:
      - name: dev.laarryy.Eris
        level: info
        AppenderRef:
          - ref: File
          - ref: RollingFile
            level: info
